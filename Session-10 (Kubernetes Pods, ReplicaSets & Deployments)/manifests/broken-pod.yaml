apiVersion: v1
kind: Pod
metadata:
  name: image-fetch-test
  labels:
    app: image-fetch-test
spec:
  containers:
    - name: nginx
      # Deliberately invalid tag: used to demonstrate ImagePullBackOff.
      image: nginx:tag-does-not-exist-for-lab
